// constants.js
const ENVIRONMENTS = {
  dev: "local",
  production: "production",
};

const URLS = {
  dev: "http://localhost:8080",
  production: "http://localhost:8080",
};

module.exports = {
  ENVIRONMENTS: ENVIRONMENTS,
  URLS: URLS,
};
